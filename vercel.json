{
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "build": {
    "env": {
      "TS_NODE_PROJECT": "tsconfig.json"
    }
  },
  "builds": [
    {
      "src": "src/**/*.ts",
      "use": "@vercel/node"
    }
  ]
}
